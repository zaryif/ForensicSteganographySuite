SYSTEM REQUIREMENTS & DEPENDENCIES

Architect: Md Zarif Azfar

WEB IMPLEMENTATION (Primary)

Browser: Chromium-based (Chrome, Edge) or Firefox. Must support Web Crypto API.

OS: Platform Agnostic.

PYTHON IMPLEMENTATION

Runtime: Python 3.8+

Dependencies:
pip install cryptography
pip install Pillow
pip install numpy

C++ IMPLEMENTATION

Compiler: GCC (g++) or Clang.

Libraries: OpenSSL (libssl-dev / openssl-devel).

Headers: stb_image.h, stb_image_write.h.

RUST IMPLEMENTATION

Toolchain: Cargo & Rustc (Stable).

Crates:
aes-gcm = "0.10"
image = "0.24"
rand = "0.8"
pbkdf2 = "0.11"
sha2 = "0.10"

SWIFT (iOS) IMPLEMENTATION

IDE: Xcode 13+.

Target: iOS 14.0+.

Frameworks: CryptoKit, CoreGraphics, UIKit.

BASH IMPLEMENTATION

Environment: Linux / macOS.

Utilities: steghide, p7zip-full, libimage-exiftool-perl.
